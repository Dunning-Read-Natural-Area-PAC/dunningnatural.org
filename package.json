{
  "scripts": {
    "tailwind": "tailwindcss --input ./src/input.css --output ./assets/css/tailwind.css --config src/tailwind.config.js",
    "clean": "rimraf public/ resources/ .wrangler/",
    "prestart": "npm run clean",
    "start": "npm run tailwind && concurrently --kill-others \"hugo --watch\" \"npm run tailwind -- --watch\" \"npx wrangler pages dev public/\"",
    "prebuild": "npm run clean",
    "build": "npm run tailwind && hugo"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.4.13",
    "wrangler": "^3.80.2"
  }
}
